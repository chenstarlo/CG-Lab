# Work2: 贝塞尔曲线与 De Casteljau 算法

基于 Taichi 框架的贝塞尔曲线绘制与交互式实验

## 实验简介

本实验实现了贝塞尔曲线的绘制，通过 De Casteljau 算法计算曲线坐标，并使用 GPU 进行高效的光栅化渲染。通过本实验，你将深入理解贝塞尔曲线的几何意义、De Casteljau 算法的递归原理以及图形学中的光栅化基础概念。

## 技术栈

- **Taichi** >=1.7.4 - 高性能并行计算框架
- **Python** >=3.12

## 项目结构

```
src/Work2/
├── __init__.py
└── main.py        # 主程序，包含 De Casteljau 算法和 GPU 绘制逻辑
```

## 核心功能

### 1. De Casteljau 算法
- 递归线性插值计算贝塞尔曲线上的点
- 支持任意数量（≥2）的控制点
- 实时计算曲线上的 1001 个采样点

### 2. GPU 光栅化渲染
- 使用 GPU 并行点亮像素
- 800x800 像素缓冲区
- 高效的批量绘制（避免 CPU-GPU 频繁通信）

### 3. 交互式控制
- 鼠标左键点击添加控制点
- 键盘 C 键清空所有控制点
- ESC 键退出程序

### 4. 可视化元素
- 红色圆点：控制点
- 灰色线条：控制多边形
- 绿色曲线：贝塞尔曲线

## 技术实现

### De Casteljau 算法原理

De Casteljau 算法是一种利用递归线性插值来求取贝塞尔曲线坐标的优雅算法：

1. **第一层插值**：对于给定的参数 `t`，在相邻控制点之间进行线性插值
   - `P' = (1-t) * P0 + t * P1`

2. **递归过程**：对生成的新点重复上述操作，直到只剩一个点

3. **终止条件**：当最终只剩下一个点时，这个点就是贝塞尔曲线在参数 `t` 处的坐标

### 光栅化基础

- **像素缓冲区**：创建 800x800 的二维数组模拟屏幕显存
- **坐标映射**：将归一化坐标 [0,1] 映射为像素索引 [0,799]
- **批量处理**：CPU 计算所有点后一次性发送到 GPU

## 快速开始

### 安装依赖

```bash
uv sync
```

### 运行程序

```bash
uv run python -m src.Work2.main
```

### 使用说明

1. 运行程序后会弹出一个 800x800 的黑色窗口
2. **鼠标左键点击**：在画布上添加控制点
3. **观察曲线生成**：添加 2 个以上控制点后会自动绘制贝塞尔曲线
4. **按 C 键**：清空所有控制点，重置画布
5. **按 ESC 键**：退出程序

## 交互演示

### 添加控制点

通过鼠标左键在画布上点击以添加控制点。每次点击后，屏幕应记录并渲染出相应的控制点（红色圆点）和控制多边形（灰色线段）。

### 观察曲线生成

程序将自动用灰线连接所有的控制点形成控制多边形，并调用 GPU 实时绘制出对应的绿色贝塞尔曲线。此时可连续添加多个点，以观察曲线随控制多边形改变而发生的几何变化。

### 清空画布

当需要重置实验环境或绘制全新曲线时，通过按下键盘的 **C 键**，程序应清空所有已记录的控制点，并将画面恢复至初始的空白状态。

## 性能优化技巧

### 1. 批量传输（Batching）

在现代图形学编程中，CPU 和 GPU 是物理分离的，它们之间通过 PCIe 总线通信。如果在 Python（CPU）的 for 循环里，每次算出一点就去修改 Taichi 的 GPU Field（如 `pixels[x, y] = 颜色`），海量的跨界通信将使帧率大幅降低，造成卡顿。

**正确的做法**：在 CPU 里把 1000 个点的坐标全算好，装进一个数组，一次性发送给 GPU，然后让 GPU 并行地把这 1000 个像素点亮。

### 2. 显存预分配（Object Pool）

现代高性能渲染管线（如 Metal/Vulkan）极其反感在主循环中"动态申请内存"。绘制辅助线时，最好不要传给它长度忽大忽小的数组，而是预先分配一个固定大小的显存池。

本程序预先分配了：
- `pixels`: 800x800 像素缓冲区
- `curve_points_field`: 1001 个曲线点
- `gui_points`: 100 个控制点

## 技术亮点

1. **De Casteljau 算法**：优雅的递归线性插值
2. **GPU 并行计算**：利用 `@ti.kernel` 装饰器实现高效像素绘制
3. **光栅化基础**：手动操作像素缓冲区，理解图形学底层原理
4. **批量传输优化**：减少 CPU-GPU 通信开销
5. **显存预分配**：避免动态内存申请，提高渲染性能
6. **实时交互**：流畅的控制点添加和曲线生成

## 数学原理

### 贝塞尔曲线

贝塞尔曲线是由一组"控制点"决定的平滑曲线。为了求出曲线上的具体坐标，我们引入一个参数 `t ∈ [0, 1]`。当 `t` 从 0 连续变化到 1 时，求出的所有点连在一起就构成了整条曲线。

### 递归公式

对于控制点 `P0, P1, ..., Pn`，贝塞尔曲线上的点 `B(t)` 可通过以下递归公式计算：

```
B(t) = (1-t) * B_0(t) + t * B_1(t)
```

其中 `B_0(t)` 和 `B_1(t)` 分别是子曲线的贝塞尔曲线。

## 预期效果

程序运行后，会在窗口中显示一个 800x800 的黑色画布。通过鼠标左键点击添加控制点，当控制点数量 ≥ 2 时，会自动生成并显示对应的贝塞尔曲线（绿色）以及控制多边形（灰色）。按 C 键可清空所有控制点，重新开始绘制。

### 视觉效果

- **黑色背景**：代表空白画布
- **红色圆点**：表示用户添加的控制点
- **灰色线段**：连接控制点形成的控制多边形
- **绿色曲线**：由 De Casteljau 算法计算生成的贝塞尔曲线

这种视觉反馈可以帮助用户直观理解控制点与贝塞尔曲线之间的关系。
